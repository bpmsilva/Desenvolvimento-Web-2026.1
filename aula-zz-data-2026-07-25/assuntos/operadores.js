// Operadores lógicas em JavaScript e comparação com Python

// Operadores lógicos
// Python: and, or, not
// JavaScript: &&, ||, !
let a = true;
let b = false;

// Comparação de operadores lógicos
let c = a && b; // Operador AND
let d = a || b; // Operador OR
let e = !a; // Negação

// Valor padrão
const nome = usuario || "Visitante";

// Executa apenas se a condição for verdadeira
idade >= 18 && console.log("Maior de idade");
